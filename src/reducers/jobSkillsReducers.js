export function skills(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export function jobs(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
